<!--TESTING AREA-->

<!----------------->


<!--from to generate random prompt (prompt shoult not save into database yet)-->
<div class="col-xs-1" align="center">
<p class="fs-1 fw-bold"> Generate a new prompt and see what you create! </p>
<form>

    <!-- Once a prompt have been generated -->
    <!---AJAX will change the buttons--->

    <!-- When button is clicked a random prompt appears-->
    <!--<button> Generate! </button>-->
    <!--------------------------------------->


    <button class="btn btn-primary"> Generate new prompt! </button>

    <% location = Faker::Address.country.downcase%>
    <% species = Faker::Creature::Animal.name.downcase%>
    <% item = Faker::Appliance.equipment.downcase%>
    
    <div >
    <% prompt = "Create a story about a #{species} in #{location} with a(n) #{item}" %>
    <p class="fs-2"><%= prompt %></p>
    </div>

    <%#= Prompt.first.full_prompt%>
  
  <!---Form to save prompt into database--->
</form>

  <form action="/create_prompts" method= post>    
    <!--If a prompt has not been generated-->
    <input type="hidden" value=<%= location %> name="location_text">
    <input type="hidden" value=<%= species %> name="species_text">
    <input type="hidden" value=<%= item %> name="item_text"> 
    <input type="hidden" value=<%= prompt %> name="prompt_text">
    <input type="hidden" value=<%= item %> name="item_text"> 
    <input type="hidden" value=<%= prompt %> name="prompt_text">

    <button class="btn btn-primary"> Choose prompt! </button>
  </form>



<!--<h2>List of all prompts (<%#= Prompt.all.count%>)</h2>
<div>
  <div>
    <table border="1">
    <tr>
      <th> Id </th>
      <th> Location </th>
      <th> Species </th>
      <th> Items </th>
      <th> Full Prompt </th>
      <th> Title </th>
      <th> Body </th>
      <th> Details </th>
    </tr>

    <%# @list_of_prompts.each do |a_prompt| %>
      <tr>
        <td> <%#= a_prompt.id %> </td>
        <td> <%#= a_prompt.location %> </td>
        <td> <%#= a_prompt.species %> </td>
        <td> <%#= a_prompt.item %> </td>
        <td> <%#= a_prompt.full_prompt %> </td>
        <td> <%#= a_prompt.title %> </td>
        <td> <%#= a_prompt.body %> </td>
        <td> <a href="/prompts/<%#=a_prompt.id %>">Show details</a></td>
      </tr>
    <%# end %>
  </div>

</div> -->
